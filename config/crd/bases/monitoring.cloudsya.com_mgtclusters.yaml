---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: mgtclusters.monitoring.cloudsya.com
spec:
  group: monitoring.cloudsya.com
  names:
    kind: MgtCluster
    listKind: MgtClusterList
    plural: mgtclusters
    singular: mgtcluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: whether cluster is reachable on the give IP and port
      jsonPath: .status.conditions[].status
      name: Reachable
      type: string
    - description: last poll timestamp(in cluster's timezone)
      jsonPath: .status.lastPollTime
      name: LastSuccessfulPollTime
      type: string
    - description: object creation timestamp(in cluster's timezone)
      jsonPath: .metadata.creationTimestamp
      name: CreatedAt
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MgtCluster is the Schema for the MgtClusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MgtClusterSpec defines the desired state of MgtCluster
            properties:
              clusterFQDN:
                description: ClusterFQDN is the Openshift/kubernetes cluster hostname
                  URL
                type: string
              email:
                description: Target user's email for cluster status notification
                type: string
              port:
                description: Port is the Openshift/kubernetes cluster port where service
                  is running
                type: string
              relayHost:
                description: Relay host for sending the email
                type: string
              suspendAlert:
                description: SuspendAlerts if set to true, target users will not be
                  notified
                type: boolean
            required:
            - clusterFQDN
            - email
            - port
            - relayHost
            type: object
          status:
            description: MgtClusterStatus defines the observed state of MgtCluster
            properties:
              conditions:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  list of status conditions to indicate the status of managed cluster
                  known conditions are 'Ready'.
                items:
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the timestamp of the last
                        update to the status
                      format: date-time
                      type: string
                    message:
                      description: Message is the human readable explanation for object's
                        condition
                      type: string
                    reason:
                      description: Reason is the machine readable explanation for
                        object's condition
                      type: string
                    status:
                      description: Status of the condition, one of ('True', 'False',
                        'Unknown')
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type of the condition, known values are 'Ready'.
                      type: string
                  required:
                  - message
                  - status
                  - type
                  type: object
                type: array
              lastPollTime:
                description: last successful timestamp of retrieved cluster status
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
